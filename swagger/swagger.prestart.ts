import { GenerateBchReactRestClient } from "./swagger.generator";

// Auto generates the bch-react-rest-client package and installs it if needed
GenerateBchReactRestClient(); // Launch the func at script init.