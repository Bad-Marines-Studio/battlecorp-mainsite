import { GenerateBchReactRestClient } from "./swagger.generator";

// Auto generates the bch-react-rest-client package with force regeneration
GenerateBchReactRestClient(true); // Launch the func at script init.